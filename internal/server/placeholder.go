// Package server provides the HTTP server (localhost only).
package server

// Placeholder ensures package is importable in the baseline scaffold.
// HTTP server will be implemented in Phase 7.
func InitServer() {}
