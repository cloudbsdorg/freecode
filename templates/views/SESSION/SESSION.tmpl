<window id="session" title="Freecode - Session">
  <vbox padding="1" gap="0">
    <text id="tabbar" value="${tabBar}" />
    <spacer height="1" />
    <text id="messages" value="${messages}" />
    <spacer flex="1" />
    <text id="input" value="${input}" />
  </vbox>
</window>
