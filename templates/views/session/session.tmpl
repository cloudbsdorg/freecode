<window title="Session" width="80" height="24">
  <vbox padding="1" gap="0">
    <text value="${model}" bold="true" />
    <spacer height="1" />
    <list id="messages" items="${messages}" />
    <spacer flex="1" />
    <input id="input" placeholder="Type your message..." />
  </vbox>
</window>
